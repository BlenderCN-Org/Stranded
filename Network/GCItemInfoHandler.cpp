#include "GCItemInfo.h"

UINT GCItemInfoHandler::Execute(GCItemInfo* pPacket, Player* pPlayer)
{
	// 物品的详细信息
	// pPacket->getID(), pPacket->getIsNull(), pPacket->getItem());
	return PACKET_EXE_CONTINUE;
}